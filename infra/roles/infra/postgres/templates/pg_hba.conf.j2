# Managed by Ansible
local   all             all                                     peer
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
{% for rule in postgres_extra_hba %}
{{ rule.type }}    {{ rule.database }}    {{ rule.user }}    {{ rule.address }}    {{ rule.method }}
{% endfor %}
